// custom shortcut keys

macro "Invert LUT [u]" {
	run("Invert LUT");
}

macro "Grayscale [g]" {
	run("Grays")
}

macro "Multi Kymogram [J]" {
	run("Multi Kymograph")
}

macro "Z Project [Q]" {
	run("Z Project...")
}

macro "Save as TIFF [6]" {
	run("Tiff...")
}

macro "Split Channels [8]" {
	run("Split Channels")
}

macro "Copy To System [0]" {
	run("Copy To System")
}
